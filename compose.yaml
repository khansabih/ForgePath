services:
  postgres:
    image: postgres:18-alpine
    container_name: forgepath-postgres

    environment:
      POSTGRES_DB: forgepath
      POSTGRES_USER: forgepath
      POSTGRES_PASSWORD: forgepath_local

    ports:
      - "5432:5432"
